﻿@using EntityLayer.Concrete
@model List<Address>

<div class="w3l_header_left">
	@foreach (var item in Model)
	{
		<ul>
			<li><span class="glyphicon glyphicon-map-marker" aria-hidden="true"></span>@item.City</li>
			<li><span class="glyphicon glyphicon-earphone" aria-hidden="true"></span>@item.PhoneNumber</li>
			<li><span class="glyphicon glyphicon-envelope" aria-hidden="true"></span> <a href="mailto:info@example.com">@item.Mail</a></li>
		</ul>
	}
</div>

@*Default Controllera ait olan Index viewda,
web sayfasında en üstte yazan adres kısmına ait olan kodları buraya keserek yapıştırırız.
Ve kodu kestiğimiz yere @await Components.Invokeasync("_TopAddressPartial") ifadesini yapıştırırız.
_TopAddressPartial ise bu default adlı partial viewın bulunduğu klasördür.
*@


@*İstersek web sayfasının en üstünde yer alan adres, telefon kısımlarını değiştirebiliriz.
Bunu da sql de yer alan Address tablosundan bu verileri çağırabiliriz.
Bunu içinde öncelikle Bu viewından oluşturulduğu classı düzenlemeliyiz.*@